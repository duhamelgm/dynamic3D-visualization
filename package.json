{
  "name": "mern-auth-app",
  "version": "1.0.0",
  "description": "A simple template for auth with the MERN stack",
  "main": "server.js",
  "scripts": {
    "dev": "concurrently \"nodemon npm start\" \"cd ./client && npm run start\"",
    "deploy": "npm install && npm install --prefix client && npm run build --prefix client && npm start"
  },
  "author": "Duhamel Guerrero",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "canvg": "^1.5.3",
    "express": "^4.16.4",
    "mongoose": "^5.4.0",
    "passport": "^0.4.0",
    "passport-jwt": "^4.0.0"
  },
  "devDependencies": {
    "concurrently": "^4.1.0",
    "nodemon": "^1.18.9"
  }
}
